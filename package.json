{
  "name": "metacoin-source-verify",
  "version": "1.0.0",
  "description": "",
  "main": "truffle-config.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "ipfs": "node --no-warnings ./scripts/ipfs.js",
    "compile": "truffle compile --all",
    "deploy:ropsten": "npm run compile && npm run ipfs && GAS_PRICE=$(CHAIN=ropsten ./gas-price.sh) truffle migrate --f 1 --to 1 --network ropsten",
    "deploy-with-salt:ropsten": "./salt.sh && npm run compile && GAS_PRICE=$(CHAIN=ropsten ./gas-price.sh) truffle migrate --f 2 --to 2 --network ropsten",
    "deploy:rinkeby": "npm run compile && npm run ipfs && GAS_PRICE=$(CHAIN=rinkeby ./gas-price.sh) truffle migrate --f 1 --to 1 --network rinkeby",
    "deploy-with-salt:rinkeby": "./salt.sh && npm run compile && GAS_PRICE=$(CHAIN=rinkeby ./gas-price.sh) truffle migrate --f 2 --to 2 --network rinkeby",
    "deploy:goerli": "npm run compile && npm run ipfs && GAS_PRICE=$(CHAIN=goerli ./gas-price.sh) truffle migrate --f 1 --to 1 --network goerli",
    "deploy-with-salt:goerli": "./salt.sh && npm run compile && GAS_PRICE=$(CHAIN=goerli ./gas-price.sh) truffle migrate --f 2 --to 2 --network goerli",
    "address": "truffle networks | grep MetaCoinSalted | sed 's/^.*MetaCoinSalted: \\(.*\\).*$/\\1/'"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cgewecke/metacoin-source-verify.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cgewecke/metacoin-source-verify/issues"
  },
  "homepage": "https://github.com/cgewecke/metacoin-source-verify#readme",
  "devDependencies": {
    "@truffle/hdwallet-provider": "1.2.2",
    "dotenv": "^8.2.0",
    "ipfs-http-client": "^42.0.0",
    "shelljs": "^0.8.3",
    "truffle": "^5.1.14",
    "web3-eth": "^1.2.9"
  }
}
